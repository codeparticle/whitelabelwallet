const POLL_INTERVAL = (60 * 1000 * 15);
const TIMESTAMP_FORMAT = 'YYYY-MM-DD h:mm:ss.SSS';

const TRANSACTION_TYPES = {
  RECEIVE: 'receive',
  SEND: 'send',
};

export const GENERAL = {
  POLL_INTERVAL,
  TIMESTAMP_FORMAT,
  TRANSACTION_TYPES,
};
