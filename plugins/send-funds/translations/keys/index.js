import { defineMessages } from 'react-intl';

export const SEND_FUNDS = defineMessages({
  NAV_ITEM: {
    id: 'plugin.send_funds.nav_item',
    description: 'Nav item label for Send Funds page',
    defaultMessage: 'Send Funds',
  },
});
